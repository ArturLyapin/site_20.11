<%= form_for(@feedback) do |f| %>
  <% if @feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>

      <ul>
      <% @feedback.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t(:name) %><br />
    <%= f.text_field :name, :class=>"name_text" %>
  </div>
 <div class="field">
    <%= f.label t(:text) %><br />
    <%= f.text_area :text%>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email,:class=>"email_text" %>
  </div>
  <div class="field">
    <%= f.label t(:phone_num) %><br />
    <%= f.number_field :phone_number, :class=>"number_text"%>
  </div>
 	<div class="field">
    <%= f.label t(:add_file) %><br />
    <%= f.file_field :document3, :class=>"document_text"%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
